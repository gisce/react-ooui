import React from "react";

import { TabManager } from "../..";

export default {
  title: "Components/Widgets/Views/TabManager",
};

export const Default = (): React.ReactElement => {
  return <TabManager />;
};
